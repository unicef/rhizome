language: python
python:
  - "2.7"
# command to install dependencies
install: pip install -r requirements.txt
# command to run tests

before_script:
  - sh -c "psql -c 'CREATE ROLE djangoapp WITH SUPERUSER;' -U postgres; fi"

script: python manage.py test --settings=rhizome.settings.test
